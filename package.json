{
  "name": "sitefolio",
  "version": "1.0.0",
  "homepage": "https://trapuntino.github.io/SITEFOLIO",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "sync": "npm run deploy && git add . && git commit -m 'Sync with deploy' && git push origin main",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.177.0"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "vite": "^7.0.5"
  }
}
